org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Set backend API URLs here (or override from command line).
# Debug (emulator/local dev): use 10.0.2.2 for host machine localhost.
API_BASE_URL_DEBUG=http://10.0.2.2:3000/api/
# Release (production APK): VPS/public backend endpoint.
API_BASE_URL_RELEASE=http://nestheaven.uz/api/

# Legacy fallback property (kept for compatibility)
API_BASE_URL=http://10.0.2.2:3000/api/

